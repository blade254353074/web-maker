var cookie = require('./cookie'),
    global = require('./global'),
    loading = require('./loading'),
    modal = require('./modal');

module.exports = {
    cookie: cookie,
    global: global,
    loading: loading,
    modal: modal
};
